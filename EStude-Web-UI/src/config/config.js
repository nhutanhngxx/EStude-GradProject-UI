const config = {
  // BASE_URL: "http://localhost:8080",
  BASE_URL: "https://estude-backend-spring.fly.dev",
  // BASE_URL: "https://estude-backend-spring.fly.dev/api/ai/health",
};

export default config;
